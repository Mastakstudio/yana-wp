include ./../mixins
doctype html
html
    +head()
    body(class="app")
        //+loader('loader__spinner_falcon')
        main.main
            +header()
            .wrapper
                +sign-in()
                +partners()
            +footer()